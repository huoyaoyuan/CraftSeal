<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="CraftSeal.Pages.SessionListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CraftSeal.Pages"
    xmlns:vm="using:CraftSeal.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView
            ItemsSource="{x:Bind DataContext.(vm:MainVM.RecentSessions)}"
            SelectionMode="None"
            IsItemClickEnabled="True"
            ItemClick="ListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:SessionVM">
                    <ListViewItem Content="{x:Bind Title}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
